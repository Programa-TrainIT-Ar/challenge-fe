import { TypeOptionsPipe } from './type-options.pipe';

describe('TypeOptionsPipe', () => {
  it('create an instance', () => {
    const pipe = new TypeOptionsPipe();
    expect(pipe).toBeTruthy();
  });
});
